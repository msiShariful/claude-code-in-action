Do the following things -

1. Check if there are any production keys in the codebase
2. Remove the production keys by asking 
3. Run `git add .`
4. Run `git commit -m "$COMMIT_MESSAGE"`
5. Run `git push`
